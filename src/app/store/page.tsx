"use client"
import AppLayout from "@/components/app-layout";

export default function Store() {
  return (
    <AppLayout>
        <main>
            <div>welcome to store</div>
        </main>
    </AppLayout>
  );
}
